# Wallpapers

Drop wallpaper images here. `setup-rice.sh` will symlink the chosen one as
`current.png` and the Hyprland autostart loads it via `swww img`.

This dir is committed for the placeholder; actual wallpaper images are
gitignored (see `swww/wallpapers/.gitignore`).
